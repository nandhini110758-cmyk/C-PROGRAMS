#include<stdio.h>
int main(){
	int number;
	printf("Enter a three digit numbers:");
	scanf("%d",&number);
	if else (num>=100 && num<=999);
	if (num%2==0)
	printf("%d is an even number\n",number);
	if else
	printf("%d is an odd number\n",number);
	return 0;
}